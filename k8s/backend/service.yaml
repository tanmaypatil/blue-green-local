apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: app
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
    version: v1  # This selector is key for blue-green switching
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
